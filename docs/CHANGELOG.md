# Changelog

Una línea por cambio, lo más reciente arriba. (Foto del presente → `ESTADO.md` · parámetros → `src/config.js` · ideas → `ideas/`.)

- 2026-06-13: render — calidad MÁXIMA = SIN LOD (a petición: "por algo se llama máxima"). Antes ultra solo BAJABA los umbrales (`lodUltraMult` 0.6) → los bichos pequeños aún se simplificaban. Ahora `ultraFull` (canvas.js `_drawAgents`): en máxima el tier es SIEMPRE grafo completo, halo y ojos para TODOS, y los gates internos de `_drawBodyGraph` (onda/señuelo/textura/gradiente/contorno) se saltan (reusa la vía `full` del retrato → `full = _forceFull || quality==='ultra'`). Todo a pelo, sin restricción (opt-in, lo más caro con 2000 agentes; no para móvil). Verificado: a zoom 1 (overview), ALTA dibuja 0 grafos completos (puntos) vs MÁXIMA 362/389 (≈todos los visibles). `lodUltraMult` queda EN DESUSO. (Alta y baja siguen con su LOD por calidad+zoom.)
- 2026-06-13: render — el LOD se DESLIGA por completo de la resolución (a petición del usuario: el LOD solo debe depender de calidad+zoom). Antes, aun sin `lodBoost`, usaba `rPx = r·sc` con `sc = coverScale·zoom`, y coverScale depende del buffer (`maxInternalPx`) → mover el slider de Resolución cambiaba el DETALLE. Ahora `rPx = r · zoom · LOD_REF` (constante fija, canvas.js): el LOD depende SOLO de CALIDAD (`lodMul`) y ZOOM, nunca de la resolución (ni nativa ni parametrizada). El dibujo real lo sigue escalando el ctx del buffer (geometría correcta); solo las DECISIONES de detalle cambian de escala. Verificado: a mismo zoom, backing 640 vs 952 → IDÉNTICO nº de grafos completos (67=67); zoom 1→0 grafos, zoom 3→65 (el zoom sí manda). Bajar la Resolución cambia nitidez, no detalle. El afinado de umbrales del LOD queda pendiente, ya 100% independiente de la resolución.
- 2026-06-13: render — auditoría del coste POR-COMPONENTE del dibujado (medido, 2000 criaturas forzadas a grafo completo): gradiente de volumen por nodo **10.8 ms** (#1), contorno/2ª pasada por nodo **8.9 ms** (#2), TEXTURA **~0** (ya bien gateada por tamaño, `rxx·ds>10`), resto (siluetas+onda+ojos+señuelo) ~25 ms. CONCLUSIÓN: el coste es DETALLE VISIBLE, no "ambición desperdiciada" — un gate SIN PÉRDIDA (saltar gradiente <5 px de buffer → relleno plano; saltar contorno <4 px, ambos invisibles a ese tamaño) solo ahorró ~1.5 ms en población grande (los nodos caros son ≥ visibles, no diminutos). Añadido ese gate igualmente en `drawNode` (lossless: ayuda en cuerpos pequeños / muchos apéndices / zoom out; verificado aspecto intacto a zoom medio). Tras LOD-by-buffer la vista general YA es barata (~2 ms, puntos); el coste del grafo solo se paga al ver muchas criaturas GRANDES, y entonces es detalle legítimo → wins reales para abaratarlo: **sprite-cache** (construir cada cuerpo 1 vez y pegarlo) o **LOD** (simplificación visible). Pendiente menor: el señuelo pinta su PROPIO halo (`hg`, radio br·7) que se solapa con el bloom global (posible recorte semi-visible).
- 2026-06-13: render — LOD ATADO AL BUFFER (se retira `renderer.lodBoost`). Decisión: **todo el render trabaja en el espacio del buffer interno; al final el CSS reescala a la pantalla** → la resolución real (4K, 8K…) NO entra en ningún cálculo. El `rPx` del LOD vuelve a ser el radio en píxeles del BUFFER (no de la pantalla). Efecto medido (buffer 640, ~1670 agentes, zoom out): 4K y pantalla pequeña dan el MISMO coste (1.86 vs 1.90 ms) y `draw()` cae de 18.9→**1.9 ms** (10×) porque las criaturas lejanas se construyen simples (puntos), no como grafos completos; en zoom in vuelven a grafo completo (detalle preservado). Bajar `maxInternalPx` ahora reduce rasterización Y construcción. TRADE-OFF aceptado: en vista general las criaturas se ven simples (campo de puntos); el detalle en la vista general pasa a ser puramente cuestión de UMBRALES del LOD (pendiente: "mejorar el montaje del LOD", ya desligado de la resolución). Verificado: sin errores, sin refs a lodBoost.
- 2026-06-12: RENDIMIENTO — dibujado BAJO DEMANDA (A) + cap de FPS (B), para descongestionar 4K + velocidad máxima. Diagnóstico medido: el coste del render es ~92% **construcción por-agente** (1643 grafos completos = 18.9 ms; rasterizar en 640 = solo 1.27 ms) → la resolución NO lo reduce (el LOD ata el detalle a la pantalla). Sin tocar el LOD aún: (A) `frame()` solo redibuja si cambió tick/cámara/selección — entre ticks el frame es idéntico (posiciones y `_animT` solo avanzan con el tick) → medido: a velocidad máxima los dibujos caen de ~33-52/s a **3/s** (libera el núcleo del hilo principal; el worker sigue pegando uno, inherente a "máx"); a 20 t/s el render va a 20 fps (= ritmo del tick, antes 120 de frames duplicados). El FPS del readout pasa a ser dibujos reales/s. (B) `render.maxFPS` (def. 60; 0 = sin límite): tope de fps del render (medido: cap 30 → 28/s; 120 → 97/s). Slider "FPS máx" en el lab junto a "Resolución" (solo laboratorio; render-only, lee en vivo, sin tocar worker/resize). Verificado en navegador: sin errores, visual intacto, 2000 agentes a 20 fps/20 t/s. PENDIENTE acordado: mejorar cómo está montado el LOD (sin atarlo a la resolución aún) y abordar C (cachear gradientes / gatear 2ª pasada+onda+ojos+señuelo por tamaño).
- 2026-06-12: UI — el control de resolución interna sale de los parámetros de simulación del lab y pasa a un slider **"Resolución" junto al botón de Calidad** (solo visible en modo laboratorio; el modo simple se mantiene limpio). Default `maxInternalPx` 1920→**1280** (render más ligero de fábrica; recuerda: cambia la nitidez, no el detalle). Aplica en vivo (`renderer.resize()`); no se persiste (vuelve al default al recargar).
- 2026-06-12: fix — el LOD (nivel de detalle) se mide por tamaño PERCIBIDO en pantalla, NO por píxeles del backing store → bajar `maxInternalPx` baja la NITIDEZ pero **NO el detalle** (antes recortaba nodos/ojos/señuelo como en calidad baja: bug). `renderer.lodBoost` reescala el `rPx` del LOD a la resolución SIN cap (el dibujo sigue a escala real). Verificado: a cap 1920 vs 640 la escala de LOD es idéntica (4.79), la real baja (4.79→3.22); a 640 con zoom se ven ojos/señuelo/cuerpo completos, solo más suave.
- 2026-06-12: fix — el tope `render.maxInternalPx` ahora se aplica también a calidad **Máxima** (antes la eximía → el slider del lab no hacía nada en ultra, parecía roto). Ultra supersamplea pero sin pasar del tope; 3840 = sin tope. (El slider afecta a FPS solo si vas render-bound; los ticks/s son de la simulación, otro hilo, e independientes de la resolución — aclarado en el tooltip.)
- 2026-06-12: render — `render.maxInternalPx` pasa de objeto por calidad `{low 960, high 1600, ultra 0}` a **escalar único (def. 1920)**, ajustable EN VIVO desde el lab (nueva categoría "🖥️ Render", slider 640–3840; dispara `renderer.resize()` vía flag `applyResize`). Sigue siendo un TECHO (min con el dispositivo → nunca sobre-renderiza pantallas pequeñas) y **exime a calidad Máxima** (conserva su supersampling). Un solo knob para el modelo mental "renderizo hasta aquí y escalo".
- 2026-06-12: RENDIMIENTO de render (móvil + 4K), Tier 1+2. Diagnóstico medido: el cuello de botella NO son los 2000 organismos sino el nº de PÍXELES del backing store (pases por píxel: bloom, sustrato, fills). (#1) **CAP de resolución interna** `render.maxInternalPx` {low 960, high 1600, ultra 0}: se renderiza por DEBAJO de la pantalla y el CSS reescala (el blur abisal disimula el upscaling) → coste por píxel ACOTADO e independiente del tamaño/DPR. Medido a igualdad de agentes (1798): 4K→1600 = **8.6× más rápido** (32.5→3.8 ms, ~263 fps). (#2) **BLOOM downsampled** (`_bloomPass`): blur de una miniatura ¼ + reescalado aditivo en vez de `ctx.filter='blur'` a pantalla completa → bloom **4.2→~0 ms**. (#3) **HALO por agente = sprite** pre-renderizado por cubo de tono (12) en vez de createRadialGradient+fill por bicho. (#4) **Ruido del SUSTRATO cacheado** (es del MUNDO, no de la cámara): se recomputa por timer (~18 frames), no en cada paneo/seguimiento → refresco **2.06→0.78 ms** amortizado. Paneo/pick usan ahora `renderer.pxRatio` (ratio real backing↔CSS), no `dpr`. Verificado: estética intacta (bloom/halos en screenshot), sin errores de consola, `node --check` + import headless OK. Descartado por ahora WebGL/OffscreenCanvas (innecesario: con el cap 4K baja a ~3.8 ms). Detalle en VISUAL.md; knob en `config.render.maxInternalPx`.
- 2026-06-12: pecera — ACTIVADA POR DEFECTO (`world.closedMatter` false→true): la app arranca en la pecera cerrada (con la receta del trío); el modelo abierto queda como alternativa (toggle del lab). + ESTUDIO DE VIABILIDAD del trío con tope 1000 (medido, 4 seeds, 9k ticks): VIABLE 3/4 con `closedRegen 0.0036 + fleeSpeed 1.0 + handlingTime 22`, PERO corre SATURADO (pop clavada en ~1000, 51-100% de los ticks) → pierde la capacidad de carga ENDÓGENA (el limitante vuelve a ser el pool, no la ecología). No hay punto dulce "pop≤1000 sin saturar + con cazadores": la productividad que alimenta a los cazadores empuja la pop sobre 1000 (closedRegen 0.0024 deja pop holgada pero 0 cazadores). DECISIÓN: se MANTIENE `maxAgents 2000` (trío 4/6, pop ~1000-2000 autorregulada por la materia, sin saturar) por preservar esa autorregulación que define la pecera.
- 2026-06-12: pecera — RED TRÓFICA completa por defecto (herbívoros + carroñeros + CAZADORES de presa viva coexisten). Supera la conclusión previa ("los cazadores de presa viva siguen ~0 en cerrado; traerlos exigiría productividad que satura"): la saturación se resuelve dando HOLGURA de pool. Ajustes de default (barrido headless multi-seed, 6-10k ticks): `world.closedRegen` 0.0017→**0.0034** (productividad para que cazar sea viable), `pop.maxAgents` 1000→**2000** (la productividad sube la pop a ~1000-2000; con 1000 saturaba), `combat.fleeSpeed` 2→**1.2** (caza algo más fácil → sostiene el nicho cazador; bajar a 1.0 lo hace boom-bust, menos robusto), `diet.scavPenalty` 0.20→**0.30** (fuerza una especie CAZADORA distinta; si no, el comecarne deriva todo a carroñero). Medido: trío estable en **4/6** siembras (cazadores = minoría ápice fluctuante ~5-40, carroñeros el grueso, herbívoros la base); el modelo ABIERTO sigue sano y con MÁS cazadores (fleeSpeed↓ → C~65). Verificado headless: sin NaN, materia conservada. `fleeSpeed`/`scavPenalty`/`maxAgents` son GLOBALES (afectan también al abierto); `closedRegen` es exclusivo de pecera. NOTA: un gremio cazador grande y rocoso pelea con la naturaleza de un mundo cerrado/magro (el carroñeo siempre será la carne fiable) → 4/6 con ápice fluctuante es el régimen "razonable" buscado.
- 2026-06-12: pecera — robustez del gremio CARNÍVORO + tope de población y siembra en el lab. Diagnóstico: los carnívoros se extinguían SOLO en pecera (el abierto sano, 3/3 seeds con comecarne ~170-350 → descartada regresión por los cambios del cerrado), en ~1/3 de las siembras (BISTABILIDAD: la cohorte sembrada crecía a ~180 y colapsaba hacia el tick 3000-5000 sin establecer nicho carroñero auto-sostenible → 0). Causa: el régimen contemplativo (closedRegen 0.0016) es un mundo magro donde el carnívoro es marginal. Ajustes: `closedRegen` 0.0016→**0.0017** (un pelín menos magro), `pop.carnivoreSeedFrac` 0.14→**0.20** (cohorte de establecimiento mayor; sigue siendo condición inicial, no estrategia codificada → la selección decide), `pop.maxAgents` 1000→**1200** (más cabecera). Validado headless (5 seeds, 20k ticks): el gremio comecarne ahora persiste **5/5** (seed1 pasó de extinto a 281 carroñeros), pop 311-977 sin saturar. MATIZ: los cazadores de presa VIVA siguen ~0 en cerrado (solo carroñeros son viables en mundo magro; traerlos exigiría productividad que satura). Expuestos en el lab (categoría "👥 Población y sembrado", ambos ↻); `maxAgents` ajustable en vivo → el worker re-asigna `speciesOf` en el reseed y `identity` (main) se sobredimensiona a 3000. Verificado en navegador: sliders OK, pop supera 1000/1200 sin romper (índices altos + clasificación de especies bien), cero errores.
- 2026-06-12: ECOSISTEMA CERRADO EN MATERIA (pecera sellada) — flag `world.closedMatter` (default OFF → modelo abierto intacto). Raíz: una auditoría de energía midió que el modelo abierto NO conserva (el sol crea biomasa de la nada + se "conjura" `carcassValue·eMax` al morir/depredar ≈17% de la entrada; la pop se clava en maxAgents=1000, no la limita la comida). El modo cerrado convierte el mundo en una pecera de MATERIA constante (moneda única): `N_libre + veg·epu + Σ(E+bodyMatter)vivos + carroña = const`. El cuerpo (`bodyMatter=carcassValue·eMax`) se RETIRA del pool N al nacer (nacer se BLOQUEA si no hay → techo ENDÓGENO) y vuelve a carroña al morir; metabolismo/nado/pérdida-trófica/conversión-pasto → N; carroña mineraliza íntegra → N; el rebrote BEBE de N (`world.closedRegen`, separado de `resource.R_regen` del abierto). Energía abierta (sol→calor), materia cerrada (real). VALIDADO headless: conserva al bit (deriva ~5e-5%, ruido float32; una fuga real —sobregiro metabólico fantasma— encontrada y corregida topando E a 0). Régimen contemplativo `closedRegen=0.0016` afinado con runs LARGOS multi-seed: pop ~610-790 estable que NUNCA satura (3/3 seeds; 0.0018 sí saturaba), con web trófico (herbívoros+carroñeros) en ~2/3 de las siembras — el modelo cerrado tiene DOS atractores (pequeño-numeroso-con-web ↔ grande-escaso-herbívoro), el seed decide → cada Sembrar varía. UI (lab): categoría "🫧 Pecera sellada" con toggle (auto-reseed), `matterBudget` (↻, def 60000) y `closedRegen` (en vivo); el nutriente libre N se muestra en la cabecera (verde) cuando está activa. Verificado en navegador: pecera ~700-830 con carroñeros, N~42k buffer, sin saturar a 46k ticks, round-trip toggle limpio, sin errores. GUARD: `energyPerUnit` es el único parámetro que rompería la conservación al tocarlo en vivo (es el tipo de cambio vegetación↔materia, entra en M=N+Σres·epu+…) → el worker ('set') ABSORBE el salto en N (`N -= Σres·Δepu`) cuando la pecera está activa; el resto de parámetros (costes/eficiencias/talla/combate) ya conservaban (medido: salto 0%, solo cambia el equilibrio). Verificado: salto de epu pasa de ±5-10% a 0.000% en todo el rango (incl. ÷5 extremo).
- 2026-06-12: móvil — REABRIR el menú arrastrando hacia ARRIBA la BARRA de velocidad (su estado COLAPSADO, siempre presente; el asa `.float-grab` es la pista visual). El gesto se engancha a TODA la barra —no solo al asa de 4px, imposible de acertar con el dedo—, igual que el de cerrar usa todo el panel; respeta el slider y los botones. Cierra la simetría (hoja ↓ cierra · barra ↑ abre); ☰ sigue como alternativa. Umbral 90px, "tirón" elástico, touch events.
- 2026-06-12: móvil — la hoja inferior del menú se OCULTA deslizándola hacia ABAJO (gesto típico de bottom-sheet), con "asa" visible (`.sheet-grab`), además del botón "contemplar". La hoja sigue al dedo; al soltar bajo umbral (90px) vuelve, pasado el umbral se oculta (reutiliza `setHidden` → aparece ☰ + velocidad flotante). Respeta sliders/botones y el scroll del laboratorio (solo arranca con `scrollTop 0`); escritorio sin cambios. Implementado con eventos TOUCH (`preventDefault()` en `touchmove` no-pasivo) → el navegador no hace scroll ni dispara `pointercancel` (con pointer events el gesto se cancelaba al instante y volvía); + `overscroll-behavior: contain`.
- 2026-06-12: nuevo modo "Colorear por" → **Rol (herbívoro / carroñero / cazador)** a 3 colores (verde/marrón/rojo). Clasifica por OFICIO dominante = `argmax(effHerb, effHunt, effScav)` (calc. en worker → byte `role`/agente; mismo criterio que la gráfica de población; los omnívoros caen en el oficio que mejor hacen). Además: la leyenda de "Colorear por" (la clave de color) ahora SÍ se muestra en laboratorio (estaba oculta en ambos modos → el selector no tenía clave); etiqueta central centrada en leyendas de 3.
- 2026-06-12: histograma — añadido el gen `scav` (Caza↔carroña) al selector (grupo Dieta), y `temp_pref` (Pref. térmica) sacado de 'Color y ornamento' a un grupo 'Térmico' propio — era FUNCIONAL (nicho térmico) pero quedaba escondido. El selector queda con los 12 genes FUNCIONALES; los visuales (color/ornamento/forma de nodos) siguen fuera (no aportan al histograma). `GENE_GROUPS` (genome.js) alimenta también el genoma del inspector.
- 2026-06-12: gráfica de población — nueva serie de CARROÑEROS (violeta `#b07be0`). La banda de comecarne (diet>0.6) se PARTE en CAZADORES (effHunt≥effScav, rojo "carn") y CARROÑEROS (effScav>effHunt, "carroñ") → disjuntas, suman el total (verificado: herb+omni+carn+carroñ=pob). Un carroñero no es otra dieta: es un comecarne que vive de cadáveres (eje `scav`), por eso la distinción es effScav>effHunt, no el gen `diet`. (worker.sampleHistory cuenta ambas; charts._drawPop: línea + leyenda de 6 entradas.)
- 2026-06-12: color por GEN — la franja del histograma no cuadraba con los organismos/leyenda (otro arco de tono → confundía: un gen a 0.5 salía morado en la franja y verde en el bicho). Unificados los TRES sitios (histograma `charts.js`, organismos `canvas.js` modo 'gene', leyenda `controls.js`) al MISMO degradado, y cambiado a **VERDE(0)→amarillo→ROJO(1)** (`hsl((1−v)·120, 80%, 52%)`; antes azul→rojo `·250`). Verificado a píxel (verde↔bajo, amarillo↔medio, rojo↔alto).
- 2026-06-12: auditoría UI — los parámetros `(UI)` están todos en el laboratorio y agrupados coherentemente. Completado el TRÍO de payoffs de morfología→nicho (antes solo `k_scavThin`): expuestos `energy.k_grazeWide` (pastador ancho) y `combat.morphReach` (cazador con alcance); + `energy.corpseReturn` (reciclaje carroña→pasto, completa los knobs de carroña). Marcados `(UI)` en config. (El laboratorio expone un superconjunto del mínimo `(UI)`; las constantes de FÍSICA de locomoción/render quedan fuera a propósito — frontera CLAUDE.md.)
- 2026-06-12: los atajos de teclado del hint ("Tecla H · Espacio") van en su propio `.kbd-hint`, ocultos en TÁCTIL (`@media (pointer: coarse)`) — irrelevantes sin teclado; antes solo se ocultaban por ancho (≤700) y se colaban en móviles anchos/tablets/horizontal. En escritorio (puntero fino) se mantienen.
- 2026-06-12: readout COMÚN a simple y laboratorio — `fps · t/s · población` (+ el total de `tick` SOLO en laboratorio, vía `.r-tick`; fuera nac/muertes del TEXTO; las gráficas de demografía del lab siguen). Ancho FIJO por valor (`padStart` + `.readout` en monospace/`white-space:pre`) → los números no "bailan" al cambiar. `pob` en azul (#5a7cd1). El modo simple ahora también muestra t/s. (Quitado `panelEl`/`adv`, ya sin uso.)
- 2026-06-12: modo simple AÚN más simple (escritorio y móvil) — fuera la gráfica de población y la fila de semilla/diversidad inicial (`#seedRow`); el modo simple es solo "ver bichos" (esos controles siguen en el laboratorio). En móvil, zoom inicial 1.8× (hacia el centro) → los organismos se ven al ENTRAR, no diminutos y de lejos. Medido en viewport móvil: menú simple al 28% (antes ~40%). Limpia las reglas de gráfica móvil del cambio anterior, ya redundantes. Solo CSS/JS de UI.
- 2026-06-12: responsive móvil — el menú SIMPLE (hoja inferior) se comía media pantalla. Compactado en móvil (popChart 84→60px, ayuda oculta, márgenes apretados, tope 48vh); en pantallas BAJAS (≤720px de alto) se oculta la gráfica. Medido en viewport móvil: 375×812 → 40% (con gráfica), 375×667 → 39% (sin), sin scroll → deja el cenote visible. Solo CSS, no toca sim ni JS.
- 2026-06-12: fix render — el RETRATO del inspector (visor de especie) recortaba detalle por LOD: su `r` es pequeño (`min(cw,ch)·0.16`) → `rPxG` caía bajo `lodLure`/`lodWave` → sin señuelo ("antenas"), sin 2ª pasada de contorno (borde fino) y sin textura. Flag `_forceFull` en `_drawBodyGraph` (lo activa `drawPortrait`) → el retrato dibuja SIEMPRE a detalle completo, coincidiendo con el organismo del mundo.
- 2026-06-12: `vercel.json` — `Cache-Control: public, max-age=0, must-revalidate` para todo (HTML/JS/CSS). Sin bundler, los ES modules se cacheaban por HEURÍSTICA del navegador y al redesplegar se mezclaban módulos nuevos+viejos → glitches en usuarios recurrentes. Ahora se revalida cada archivo en cada carga (304 si no cambió vía ETag, fresco tras un deploy) → versión SIEMPRE consistente, sin cache-busting manual.
- 2026-06-12: GUSANO carroñero (Fase 2) — gen `scav` (genoma 185→186) reparte la capacidad carnívora entre CAZAR presa viva (`effHunt`) y CARROÑEAR cadáveres (`effScav`), con `diet.scavPenalty` (especializa) y `energy.k_scavThin` (carroñear rinde más con cuerpo FINO). El carroñero navega hacia la carroña con el gradiente de comida ya DEPENDIENTE de dieta (effHerb·∇recurso + effScav·∇carroña; sin nueva entrada al cerebro). Siembra: media cohorte comecarne como proto-gusano (cadena axial de nodos + cuerpo barato). Medido headless: el carroñero EMERGE pequeño y elongado (`elongN`~1.7, persiste 4/4 a 12k) y coexiste con cazadores y herbívoros. LECCIÓN: el nicho da divergencia ecológica (carroñero pequeño) pero una morfología COMPLEJA (elongada) vive en un valle de fitness → hay que SEMBRAR la proto-forma para cruzarlo (como los proto-carnívoros cruzan el de la dieta); cruzado, el cuerpo se mantiene por inercia + streamlining (k_scavThin es refuerzo, no sostén). 3 knobs en UI.
- 2026-06-11: CARROÑA (Fase 1, hacia el nicho carroñero/gusano) — campo `carrion` por celda (world.js): toda muerte deja CUERPO según causa (vejez/hambre/combate = cuerpo entero = E + biomasa `carcassValue·eMax`; cazado = solo sobras `scrapReturn·biomasa`); decae cada tick (`resource.carrionDecay`) devolviendo `corpseReturn` al pasto (ciclo de nutrientes); lo consume quien come carne (`effCarn`, ritmo `resource.carrionAbsRate`). Render: mancha gris desaturada en la celda, opacidad ∝ carroña (canvas.js). Centralizado en `_kill` (sustituye el viejo `_depositCorpse`→pasto). Medido headless (R_regen 0.0035): el carroñeo multiplica los carnívoros (×6 a absRate 0.30) → default suave 0.15. Knobs en UI. Fase 2 (carroñeo como eje de dieta propio → gusano) pendiente.
- 2026-06-11: `energy.carcassValue` (def. 0.25, UI) — la caza rinde `preyGain·(E_presa + carcassValue·eMax_presa)`: el cuerpo vale su BIOMASA además de su energía almacenada. ARREGLA por qué cazar era imposible al bajar la comida: la presa cría hasta el tope pero MAGRA (eFracH 0.65→0.35) → cazarla daba calorías vacías → carnívoros extintos rodeados de presa abundante (medido headless: a R_regen 0.003, 3/4 semillas extinguían carnívoros con carcassValue=0; con 0.25, 0/4 y minoría estable ~6-13%). Aditivo (conserva el gradiente presa-gorda↔magra = freno L-V parcial); subirlo mucho ablanda el freno → oscilaciones en comida abundante.
- 2026-06-11: render — la animación de los nodos (ondear/aletear) sigue ahora la velocidad ABSOLUTA (÷ `loco.vMax`), no la fracción de la capacidad propia → los lentos ondean suave y los rápidos baten fuerte (antes todos iban a tope nada más moverse: "demasiado rápido para lo poco que se desplazan"). `freq/amp` recalibradas en canvas.js.
- 2026-06-11: lab — retirada la monitorización de carnívoros (índice de cazabilidad + autopsia); demografía de TODO el ecosistema en DOS gráficas: nacimientos (sexual/asexual) y muertes (cazado/atacando/hambre/vejez). La gráfica de tipos añade la población TOTAL en azul (el del readout 'pob').
- 2026-06-11: retirado el match color↔luz (`color.matchPenalty` + campo `lightHue`) — era selección INVISIBLE (el campo de luz nunca se dibujó); el color (`hue`) pasa a gen neutro/linaje. Fuera del bucle de pasto, config y UI.
- 2026-06-11: la velocidad por fin importa — `combat.fleeSpeed=2` (escape por velocidad relativa) + `refuge.strength` 0.9→0.3 → huir/cazar es un duelo de velocidad → la vmax sube por morfología propulsora. (UI). Antes era ~neutra (forrajear no es carrera; medido: ni comida ni cobertura la movían).
- 2026-06-11: `repro.asexual` se mantiene ON — comprobado headless que SOLO-sexual aplana la diversidad de talla (la mezcla regresa los extremos a la media); el payoff de talla luce con asexual.
- 2026-06-11: forrajeo por talla (`resource.forageReach`, def. 3) + `omniPenalty` 0.05→0.15 — el grande pasta de un área → payoff a la talla → DOS grupos de talla (peque+grande) robustos. Fine-tuning headless (5 semillas, head-to-head). (UI)
- 2026-06-11: (A) coste de transporte ∝ masa (`energy.k_haul`) — nadar un cuerpo grande/con más apéndices gasta más, no solo mantenerlo. (UI)
- 2026-06-11: `combat.preyBandLo` (suelo de banda de caza) expuesto en la UI — talla mín. y máx. de presa cazable ya se controlan por separado.
- 2026-06-11: `headThrust` 0.15→0.06 — nadar exige propulsores (los nodos frontales frenan, no propulsan).
- 2026-06-11: docs reestructuradas — retirado `CONFIG.md` (config.js fuente única); ideas → `ideas/` (índice+fichas+archivo); CHANGELOG; AUDIT a `archivo/`.
- 2026-06-11: morfología Capas 1-3 — `tipShape` (forma), `morphReach`/`k_grazeWide` (función), `gaitMode`+`flapCost` (movimiento); genoma 169→185.
- 2026-06-11: 3ª calidad "Máxima" (doble bloom, supersampling, +nieve); botón Reiniciar a la cabecera (ámbar).
- 2026-06-11: `diet.omniPenalty` 0→0.05 (dial de especialización).
- 2026-06-10: eliminado el escenario pradera → Cenote abisal único; quitadas las estelas.
- 2026-06-10: modo simple simplificado; gráfica de población por dieta.
- 2026-06-10: SPEC/VISUAL reescritos al modelo de nodos (deuda v2.0 saldada).
- 2026-06-09: auditoría evolutiva CERRADA (14/14) → `archivo/AUDIT_EVOLUCION.md`.
