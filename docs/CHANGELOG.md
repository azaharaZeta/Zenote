# Changelog

Una línea por cambio, lo más reciente arriba. (Foto del presente → `ESTADO.md` · parámetros → `src/config.js` · ideas → `ideas/`.)

- 2026-06-12: móvil — REABRIR el menú arrastrando hacia ARRIBA la BARRA de velocidad (su estado COLAPSADO, siempre presente; el asa `.float-grab` es la pista visual). El gesto se engancha a TODA la barra —no solo al asa de 4px, imposible de acertar con el dedo—, igual que el de cerrar usa todo el panel; respeta el slider y los botones. Cierra la simetría (hoja ↓ cierra · barra ↑ abre); ☰ sigue como alternativa. Umbral 90px, "tirón" elástico, touch events.
- 2026-06-12: móvil — la hoja inferior del menú se OCULTA deslizándola hacia ABAJO (gesto típico de bottom-sheet), con "asa" visible (`.sheet-grab`), además del botón "contemplar". La hoja sigue al dedo; al soltar bajo umbral (90px) vuelve, pasado el umbral se oculta (reutiliza `setHidden` → aparece ☰ + velocidad flotante). Respeta sliders/botones y el scroll del laboratorio (solo arranca con `scrollTop 0`); escritorio sin cambios. Implementado con eventos TOUCH (`preventDefault()` en `touchmove` no-pasivo) → el navegador no hace scroll ni dispara `pointercancel` (con pointer events el gesto se cancelaba al instante y volvía); + `overscroll-behavior: contain`.
- 2026-06-12: modo simple AÚN más simple (escritorio y móvil) — fuera la gráfica de población y la fila de semilla/diversidad inicial (`#seedRow`); el modo simple es solo "ver bichos" (esos controles siguen en el laboratorio). En móvil, zoom inicial 1.8× (hacia el centro) → los organismos se ven al ENTRAR, no diminutos y de lejos. Medido en viewport móvil: menú simple al 28% (antes ~40%). Limpia las reglas de gráfica móvil del cambio anterior, ya redundantes. Solo CSS/JS de UI.
- 2026-06-12: responsive móvil — el menú SIMPLE (hoja inferior) se comía media pantalla. Compactado en móvil (popChart 84→60px, ayuda oculta, márgenes apretados, tope 48vh); en pantallas BAJAS (≤720px de alto) se oculta la gráfica. Medido en viewport móvil: 375×812 → 40% (con gráfica), 375×667 → 39% (sin), sin scroll → deja el cenote visible. Solo CSS, no toca sim ni JS.
- 2026-06-12: fix render — el RETRATO del inspector (visor de especie) recortaba detalle por LOD: su `r` es pequeño (`min(cw,ch)·0.16`) → `rPxG` caía bajo `lodLure`/`lodWave` → sin señuelo ("antenas"), sin 2ª pasada de contorno (borde fino) y sin textura. Flag `_forceFull` en `_drawBodyGraph` (lo activa `drawPortrait`) → el retrato dibuja SIEMPRE a detalle completo, coincidiendo con el organismo del mundo.
- 2026-06-12: `vercel.json` — `Cache-Control: public, max-age=0, must-revalidate` para todo (HTML/JS/CSS). Sin bundler, los ES modules se cacheaban por HEURÍSTICA del navegador y al redesplegar se mezclaban módulos nuevos+viejos → glitches en usuarios recurrentes. Ahora se revalida cada archivo en cada carga (304 si no cambió vía ETag, fresco tras un deploy) → versión SIEMPRE consistente, sin cache-busting manual.
- 2026-06-12: GUSANO carroñero (Fase 2) — gen `scav` (genoma 185→186) reparte la capacidad carnívora entre CAZAR presa viva (`effHunt`) y CARROÑEAR cadáveres (`effScav`), con `diet.scavPenalty` (especializa) y `energy.k_scavThin` (carroñear rinde más con cuerpo FINO). El carroñero navega hacia la carroña con el gradiente de comida ya DEPENDIENTE de dieta (effHerb·∇recurso + effScav·∇carroña; sin nueva entrada al cerebro). Siembra: media cohorte comecarne como proto-gusano (cadena axial de nodos + cuerpo barato). Medido headless: el carroñero EMERGE pequeño y elongado (`elongN`~1.7, persiste 4/4 a 12k) y coexiste con cazadores y herbívoros. LECCIÓN: el nicho da divergencia ecológica (carroñero pequeño) pero una morfología COMPLEJA (elongada) vive en un valle de fitness → hay que SEMBRAR la proto-forma para cruzarlo (como los proto-carnívoros cruzan el de la dieta); cruzado, el cuerpo se mantiene por inercia + streamlining (k_scavThin es refuerzo, no sostén). 3 knobs en UI.
- 2026-06-11: CARROÑA (Fase 1, hacia el nicho carroñero/gusano) — campo `carrion` por celda (world.js): toda muerte deja CUERPO según causa (vejez/hambre/combate = cuerpo entero = E + biomasa `carcassValue·eMax`; cazado = solo sobras `scrapReturn·biomasa`); decae cada tick (`resource.carrionDecay`) devolviendo `corpseReturn` al pasto (ciclo de nutrientes); lo consume quien come carne (`effCarn`, ritmo `resource.carrionAbsRate`). Render: mancha gris desaturada en la celda, opacidad ∝ carroña (canvas.js). Centralizado en `_kill` (sustituye el viejo `_depositCorpse`→pasto). Medido headless (R_regen 0.0035): el carroñeo multiplica los carnívoros (×6 a absRate 0.30) → default suave 0.15. Knobs en UI. Fase 2 (carroñeo como eje de dieta propio → gusano) pendiente.
- 2026-06-11: `energy.carcassValue` (def. 0.25, UI) — la caza rinde `preyGain·(E_presa + carcassValue·eMax_presa)`: el cuerpo vale su BIOMASA además de su energía almacenada. ARREGLA por qué cazar era imposible al bajar la comida: la presa cría hasta el tope pero MAGRA (eFracH 0.65→0.35) → cazarla daba calorías vacías → carnívoros extintos rodeados de presa abundante (medido headless: a R_regen 0.003, 3/4 semillas extinguían carnívoros con carcassValue=0; con 0.25, 0/4 y minoría estable ~6-13%). Aditivo (conserva el gradiente presa-gorda↔magra = freno L-V parcial); subirlo mucho ablanda el freno → oscilaciones en comida abundante.
- 2026-06-11: render — la animación de los nodos (ondear/aletear) sigue ahora la velocidad ABSOLUTA (÷ `loco.vMax`), no la fracción de la capacidad propia → los lentos ondean suave y los rápidos baten fuerte (antes todos iban a tope nada más moverse: "demasiado rápido para lo poco que se desplazan"). `freq/amp` recalibradas en canvas.js.
- 2026-06-11: lab — retirada la monitorización de carnívoros (índice de cazabilidad + autopsia); demografía de TODO el ecosistema en DOS gráficas: nacimientos (sexual/asexual) y muertes (cazado/atacando/hambre/vejez). La gráfica de tipos añade la población TOTAL en azul (el del readout 'pob').
- 2026-06-11: retirado el match color↔luz (`color.matchPenalty` + campo `lightHue`) — era selección INVISIBLE (el campo de luz nunca se dibujó); el color (`hue`) pasa a gen neutro/linaje. Fuera del bucle de pasto, config y UI.
- 2026-06-11: la velocidad por fin importa — `combat.fleeSpeed=2` (escape por velocidad relativa) + `refuge.strength` 0.9→0.3 → huir/cazar es un duelo de velocidad → la vmax sube por morfología propulsora. (UI). Antes era ~neutra (forrajear no es carrera; medido: ni comida ni cobertura la movían).
- 2026-06-11: `repro.asexual` se mantiene ON — comprobado headless que SOLO-sexual aplana la diversidad de talla (la mezcla regresa los extremos a la media); el payoff de talla luce con asexual.
- 2026-06-11: forrajeo por talla (`resource.forageReach`, def. 3) + `omniPenalty` 0.05→0.15 — el grande pasta de un área → payoff a la talla → DOS grupos de talla (peque+grande) robustos. Fine-tuning headless (5 semillas, head-to-head). (UI)
- 2026-06-11: (A) coste de transporte ∝ masa (`energy.k_haul`) — nadar un cuerpo grande/con más apéndices gasta más, no solo mantenerlo. (UI)
- 2026-06-11: `combat.preyBandLo` (suelo de banda de caza) expuesto en la UI — talla mín. y máx. de presa cazable ya se controlan por separado.
- 2026-06-11: `headThrust` 0.15→0.06 — nadar exige propulsores (los nodos frontales frenan, no propulsan).
- 2026-06-11: docs reestructuradas — retirado `CONFIG.md` (config.js fuente única); ideas → `ideas/` (índice+fichas+archivo); CHANGELOG; AUDIT a `archivo/`.
- 2026-06-11: morfología Capas 1-3 — `tipShape` (forma), `morphReach`/`k_grazeWide` (función), `gaitMode`+`flapCost` (movimiento); genoma 169→185.
- 2026-06-11: 3ª calidad "Máxima" (doble bloom, supersampling, +nieve); botón Reiniciar a la cabecera (ámbar).
- 2026-06-11: `diet.omniPenalty` 0→0.05 (dial de especialización).
- 2026-06-10: eliminado el escenario pradera → Cenote abisal único; quitadas las estelas.
- 2026-06-10: modo simple simplificado; gráfica de población por dieta.
- 2026-06-10: SPEC/VISUAL reescritos al modelo de nodos (deuda v2.0 saldada).
- 2026-06-09: auditoría evolutiva CERRADA (14/14) → `archivo/AUDIT_EVOLUCION.md`.
