# Changelog

Una línea por cambio, lo más reciente arriba. (Foto del presente → `ESTADO.md` · parámetros → `src/config.js` · ideas → `ideas/`.)

- 2026-06-17: «Diversidad inicial» (`pop.startDiversity`) recentrada — default 0→**0.5** (la diversidad moderada pasa a ser el medio del slider). A **0.0 = fundadores TODOS IGUALES**: renacuajos herbívoros idénticos y básicos (fenotipo byte-idéntico, medido: radio/vmax/dieta spread 0, 2 nodos cabeza+cola; la variación restante es latente/no expresada). El cohorte proto-carnívoro ahora ESCALA con la diversidad (`carnFrac = min(1, div·2)` → nulo a div=0, pleno a div≥0.5 → el default conserva la siembra carnívora actual). VIABLE a 0.0 (solo-herbívoro): medido 6/6 seeds a 20k (pop 414-807, no se extingue). En `config.js` + `sim.js _seedSimple`. SPEC §siembra.

- 2026-06-17: UI — el histograma («Histograma de») admite **VELOCIDAD** además de los genes: nueva opción «Velocidad» (grupo Movimiento) que binea la velocidad ABSOLUTA realizada de cada organismo (`|v|/vMax` ∈[0,1]); el worker la calcula con un índice centinela `<0` (no es un gen). Coherente con el modo color 'gene' (entonces colorea por velocidad) y su leyenda. En `worker.js` (binning), `charts.js` (rótulo) y `controls.js` (opción + leyenda).

- 2026-06-17: SEMBRADO inicial FIJO + CÍRCULO CENTRAL — `pop.initial` (def **150**) ya NO escala con `world.size` (era ×área, Modelo A): nº FIJO de fundadores, sembrados en un CÍRCULO CENTRAL de densidad fija (nuevo `pop.seedDensity`, no-ui; área del disco = `initial/seedDensity`, independiente del mundo → colonizan hacia fuera; mundo pequeño → el disco se acota al mundo). Muestreo uniforme en área (r·√u). En `sim.js` (`_initialCount` sin ×_aScale, `_seedZoneRadius`, posiciones en disco) + UI (el slider «Sembrado inicial» pierde la anotación de escalado). `matterBudget`/rejilla siguen escalando con el área. SPEC §1.

- 2026-06-17: ESFUERZO = SALIDA DEDICADA del cerebro (4ª salida) — antes el esfuerzo era el MÓDULO del vector de deseo (dirección y fuerza ACOPLADAS); con los inputs direccionales normalizados a unidad, el `tanh` se saturaba → throttle clavado ~0.9 para casi todos (medido: 0% se paraba, sin modular por energía → "todos van a su máxima y la misma velocidad"). Ahora el cerebro emite el ESFUERZO en una salida independiente de la dirección (`BRAIN.O` 3→4, `NUM_GENES` 202→208; salida 2=ataque sin mover, 3=esfuerzo; sembrada a throttle base ~0.7 competente, la MODULACIÓN emerge —no se siembra—). Medido headless (8-25k): throttle media 0.9→0.63-0.71, SD ~0.3, **3-11% se PARAN**, 18-26% van despacio; en vivo (tick 6611): 11% lentos / 56% medios / 33% rápidos. Ecología MÁS sana (6/6 seeds, media pop ~440-680→1089: descansar ahorra energía → más biomasa sostenible). En `genome.js` (BRAIN.O, seedBrain) + `sim.js` (_brain 4ª salida, movimiento). SPEC §2bis/§cerebro · mem. `locomocion-modelo-fuerza`.

- 2026-06-17: FIX cableado render↔worker (ALETEO) — el `spd` que controla la frecuencia/amplitud de la ondulación volvió RELATIVO a la capacidad propia (÷ vmax del agente) con el cambio de zancada, pero el render seguía calibrado (×6) para `spd` ABSOLUTO → casi todo organismo activo (a ~90% de SU tope) aleteaba a ~6.5× aunque avanzara despacio ("aletea rápido pero no se mueve"). Ahora `spd = |v| / vMax global` (`worker.js`, mundo + retrato del inspector) → el aleteo acompaña a la traslación REAL (lento = ondea suave, rápido = bate fuerte). Medido: spd medio 0.9→0.14, 0% en aleteo frenético. El diagnóstico descartó el giro físico como causa (eff de trayectoria 0.83 = van rectos, no se revuelven). Solo render; no toca la simulación. Resuelve el item de bandeja "animación de nado ligada al esfuerzo real".

- 2026-06-17: GIRO FÍSICO (nivel B de la idea «giro físico») — el rumbo ya no gira de forma cinemática instantánea, sino con INERCIA ANGULAR: una vel. angular `omega` (estado nuevo) se acerca al objetivo (`turnRate·error`, capado a ±turnRate) con lag `angResp = 1/(1+angInertia·max(0,masa−1))` → los cuerpos grandes/complejos tardan en girar y sobregiran/contragiran; los ligeros giran casi al instante. Nuevo `loco.angInertia` (UI «Inercia de giro», def 0.5; =0 → modelo previo). En `organism.js` (angResp) + `sim.js` (omega, bucle de movimiento). Medido headless: faithful (angInertia=0 ≈ baseline vector-lerp, mismo ballpark), ESTABLE (|omega| acotado a ±turnRate, sin spin-out), ecológicamente SEGURO (6/6 seeds vivos, diversidad de gremios comparable a 25k). Cerebro intacto. SPEC §2bis · mem. `locomocion-modelo-fuerza`. (Nivel C ya estaba; A —cerebro izq/der— queda como opción futura arriesgada.)

- 2026-06-17: gen `speed` REPURPOSADO a MUSCULATURA — en el modelo de fuerza `speed` estaba inerte (el esfuerzo lo decide el cerebro); ahora es INVERSIÓN MUSCULAR: escala la capacidad de empuje (`vmax` ×, `loco.muscleMin/Max`) y cuesta basal mantenerla (`energy.k_muscle`, ∝ exceso sobre el neutro) → el cerebro decide cuánto USA (trade-off r/K: músculo sin usar = caro). Medido headless (25k): la musculatura DIVERGE por nicho desde el sembrado neutro 0.5 → carroñeros ~0.2 (lentos/baratos), herbívoros/cazadores ~0.6 (rápidos) → gen bajo selección, emergente. Etiqueta de gen «Musculatura». Frontera en `organism.js`; SPEC §2bis · ficha en `ideas/archivo/`.

- 2026-06-17: AUDITORÍA de código + doc (sesión) — FIX tope de población (`identity` del render se reajusta al Reiniciar vía `sim.cap` en el mensaje `world`; el slider `maxAgentsCeiling` es ↻, hasta 8000 → ya no deja agentes fuera de rango; verificado en navegador). Render robusto: `try/finally` en `drawPortrait` (no deja `_forceFull` activo si el retrato lanza → evita detalle completo global silencioso). UI: añadidos los 2 sliders `(UI)` que faltaban — `loco.dragLin` (arrastre/inercia) y `loco.speedSizeExp` (zancada por talla). DOC: corregidas discrepancias doc↔código (recuentos de genoma/cerebro → punteros a `NUM_GENES`/`BRAIN`, defaults de render → `config.js`, estelas retiradas, `_regenClosed`→`regen()`, comentarios de `dragLin`/`world.size`/hash) y la caracterización de la conservación (fuga `Float32` SISTEMÁTICA en la alimentación herbívora —al acreditar `E`/`N`—, no "ruido sin sesgo"; acotada y despreciable).

- 2026-06-17: LOCOMOCIÓN — VELOCIDAD relativa a la TALLA (zancada): `vmax_mundo ∝ (radio/medio)^speedSizeExp` (0.5) + ondulación del render relativa al vmax propio. El grande da zancadas mayores; el pequeño es rápido en su escala pero se desplaza poco. WIN ecológico (↑diversidad de talla, ↓dominancia carnívora). SPEC §2bis · mem. `locomocion-modelo-fuerza`.

- 2026-06-17: LOCOMOCIÓN — modelo de FUERZA (control por ESFUERZO): el cerebro decide el ESFUERZO (módulo de su salida) y la dirección; la velocidad EMERGE de empuje−arrastre con inercia ∝masa (+propiocepción como entrada del cerebro). Re-balanceado (`carcassValue 0.14` + `refuge 0.45`). SPEC §2bis · mem. `locomocion-modelo-fuerza`.

- 2026-06-16: UI — rediseño de la página principal (intro/portada).

- 2026-06-16: SEÑUELO rediseñado — desacoplado de la selección sexual (`orn`); gen propio `o_len`/`o_bulb` con gate (`combat.lureGate`) → lo expresa solo ~la fracción cazadora, no universal → nicho de emboscada real (anglerfish). SPEC §3 · mem. `simplificacion-ablacion-2026-06`.

- 2026-06-16: SIMPLIFICACIÓN / poda (medida antes de podar) — eliminados del código: modo mundo-abierto (solo queda la pecera cerrada), `wrap` opcional (toro siempre), nicho térmico, gen de ángulo de visión y genes decorativos de bajo impacto (`c_eye`/`c_sat`/`tex2`), más código muerto. mem. `open-mode-removed-closed-only`, `simplificacion-ablacion-2026-06`.

- 2026-06-16: LIMPIEZA de comentarios — condensados los comentarios verbosos (multi-línea, con historial de tuning) a una línea breve y explicativa en TODOS los archivos fuente (`config.js`, `engine/*`, `render/canvas.js`, `ui/*`, `main.js`). Solo comentarios: CERO cambio de código. Verificado: checksum del motor byte-idéntico (1500/4000t), smoke OK, sintaxis OK en los 12 archivos, render sin errores en preview. El detalle/historial sigue en CHANGELOG, SPEC y la memoria del proyecto (no en el código). Los `d:` (tooltips de la UI, texto de usuario) se conservan.

- 2026-06-15: UI laboratorio — (1) RETIRADOS los 3 parámetros que solo aplican al modelo ABIERTO (la pecera es permanente): `resource.R_regen`, `energy.corpseReturn`, `energy.scrapReturn` ya NO salen en la UI en ningún caso (siguen en `config.js` por si se reactiva el abierto). Retirado también el mecanismo de "atenuar por modo" (`modeGated`/`refreshModeGating` + CSS `.lab-inert`), ya sin uso. (2) REAGRUPADO: se desmonta el cajón de sastre "⚡ Energía y costes" (17 params dispares) repartiendo cada uno a su grupo semántico → costes de nado (`k_effort`/`moveCost`/`k_haul`/`k_drag`) a 🏊 Locomoción; energética de depredación (`preyGain`/`carcassValue`) a ⚔ Combate; ingreso de pasto por forma (`k_graze`/`k_grazeWide`) a 🍃 Comida; carroñeo (`k_scavThin`) a la nueva 💀 Carroña. El resto queda como ⚡ Metabolismo y cuerpo. Nueva estructura (11 grupos): 🌍 Mundo y población · 🍃 Comida y vegetación · 💀 Carroña · ⚡ Metabolismo y cuerpo · 🏊 Locomoción y visión · 🥚 Reproducción · 🧬 Mutación · ⬡ Edad y longevidad · ⚔ Combate y dieta · 🌿 Refugio · 🎨 Estética. Solo UI (mismas claves/controles, 67; verificado: 0 perdidos/duplicados, sin errores).

- 2026-06-15: UI — el marcador **↻** (parámetros que requieren Reiniciar) ahora va en DORADO/ámbar (`#e0a04a`, mismo color que el botón ↻ Reiniciar → coherencia de significado), separado del texto de la etiqueta en su propio `<span class="reseed-mark">` (`styles.css` + `controls.js`); también la ↻ de la leyenda del laboratorio (`index.html`). Solo estético.

- 2026-06-15: RENDIMIENTO render a ESCALA (mundos grandes / miles de agentes) — el LOD (nivel de detalle) ahora escala con `world.size`: `lodSc = LOD_REF · zoom · (WORLD_REF/world.size)` (antes `LOD_REF · zoom`, ignoraba el tamaño del mundo). Diagnóstico medido: a `world.size` 3000 y zoom-1 (vista del mundo entero) cada organismo ocupa ~1-2 px EN PANTALLA, pero el LOD lo trataba como tier-2 y dibujaba el GRAFO completo de nodos (detalle invisible) → ~25 µs/agente · ~97 ms/frame a 4654 agentes (~10 fps). Con el factor de mundo, esos agentes sub-píxel caen a tier-0/1 (punto/elipse) → **16.5× más barato por agente · ~6 ms/frame (fluido)**. A `world.size`=1000 (default) es **byte-idéntico al histórico** (factor 1, los umbrales `lod*` se calibraron ahí); al ACERCAR el zoom en un mundo grande los agentes vuelven a tier-2 (detalle completo cuando de verdad se ven). Solo render: NO toca la simulación (checksum del motor idéntico) ni la trayectoria. Sigue SIN depender de la resolución (`maxInternalPx`/DPR): es `world.size`, no píxeles de buffer. (Pendiente afín: el snapshot del worker aún empaqueta los nodos `n·80` por frame aunque a zoom-out no se usen → optimización futura.)
- 2026-06-15: MORFOLOGÍA — SEMBRADA la GARRA del CAZADOR (`sim.js _seedSimple`: cohorte cazadora PAR recibe un par de apéndices frontales — `emit≈0` → suman a `plan.fwdReach` → alcance de captura `morphReach`), análoga al proto-gusano de los carroñeros. Diagnóstico: medida la FORMA por gremio al régimen sano, el herbívoro emerge ANCHO y el carroñero FINO (gusano) ✅, pero el cazador NO desarrollaba apéndices de agarre (`fwdReach`~0.04, cuerpo grande indistinguible de un herbívoro) — y subir `combat.morphReach` ×4 NO lo inducía (VALLE de fitness: la garra frena el nado antes de que el alcance pague). Fix = sembrar la proto-forma (no es un parámetro). Medido (5 seeds, 20k): la garra PERSISTE por selección allá donde sobrevive el gremio cazador (`fwdReach` 3k→20k 1.24→1.33, 97-100% de cazadores con garra en 3/3 seeds-con-cazador; se erosiona solo donde el comecarne deriva a carroñero = correcto, un carroñero no la necesita). Emerge la 3ª silueta. Coste: la garra hace al cazador más eficaz → sesgo leve a más cazadores (25→31%) y 1/5 seeds tipa a sobre-caza (media sigue sana, herb 33%). `morphReach` se queda en 0.4 (basta para MANTENER la garra una vez sembrada). Es bootstrapping legítimo (como gusano/proto-carnívoros), la forma sigue evolucionando.
- 2026-06-15: GREMIOS (Bucle 4) — `energy.carcassValue` 0.25→**0.20**. Diagnóstico que destapó el problema: al medir la estructura trófica COMPLETA del régimen 4.0/0.0055 (no solo "cazadores presentes"), 5/6 seeds estaban COLAPSADAS a monocultivo comecarne (dieta media 0.83, herbívoros 0%) — la métrica "huntSeeds" contaba esos mundos todo-carnívoro como sanos (error de métrica: optimizaba el ápice sin mirar la base). Causa: `carcassValue` alto hace la carne tan rentable que la dieta BARRE a carnívora y la depredación SUPRIME a los herbívoros (estado estable alternativo). Fix medido (barrido `refuge`×`carcassValue` + validación 25k/6 seeds): bajar a 0.20 restaura la base (herb 38%) con el TRÍO completo (herb+carroñero+cazador 4/6, estable a 25k). HALLAZGO clave: el balance de gremios es BIMODAL/no-monótono (0.18 salió PEOR que 0.15 y 0.20) → no se puede afinar con precisión, se eligen valores robustos. (refuge 0.30 sin tocar; 0.15 sería un mundo más herbívoro con cazadores casi ausentes.)
- 2026-06-15: RÉGIMEN re-parametrizado con DATOS (no con comentarios heredados) — `expr.size.min` 2.0→**4.0** y `world.closedRegen` 0.004→**0.0055**, fijados JUNTOS. Precedido de `docs/ANALISIS_PARAMETROS.md` (mapa de interacciones derivado de las ecuaciones del motor) + barrido 2D `size.min`×`closedRegen` + validación 25k/6 seeds. Hallazgos: (1) la PRODUCTIVIDAD (`closedRegen`) es el motor del cazador, no `size.min`; (2) `size.min` DES-satura el pool pero hace falta ~4.0 (a 3.0/3.4 aún satura) — el viejo "3.4 recomendado" resultó BORDE (2/6 cazadores, 1/6 satura), no un valor medido; (3) el combo 4.0+0.0055 da cazador robusto (4/6 seeds), pool con holgura (poolFrac 0.21 ESTABLE 15k→25k, 0/6 satura) y patchiness alta (CV 0.38). Mecanismo (Bucle 2): a `size.min` alto los cuerpos son caros → pop materia-limitada → la productividad va a la cadena trófica, no a un enjambre r que satura. 4.5 da 6/6 cazadores pero comprime la diversidad de talla → se eligió 4.0 por equilibrio. Conserva (deriva <0.3%). Retirada la "Nota temporal 2.0".
- 2026-06-15: PROBADO y DESCARTADO — `vegDecay` (mortalidad/senescencia del pasto): idea del análisis de vegetación (el pasto no tiene turnover propio, solo sale por pastoreo). Implementada (`World.decayVegetation`, conservativa: pasto en pie → `N` local) y medida multi-seed (pecera, 25k). Resultado NEGATIVO: subirla es CONTRAPRODUCENTE a largo plazo (con `expr.size.min`=2.0) — libera al pool `N` la materia bloqueada en pasto en pie → rebrote más fiable → SUBE la capacidad de carga efectiva → más saturación del pool de r-estrategas → patchiness ↓ (CV 0.26→0.11) y el gremio CAZADOR colapsa (3/4→1/4 seeds; pob 1574→1998). REVERTIDA (no aportaba con el config actual; re-añadible en ~17 líneas si se restaura `size.min`=3.4, donde queda sin medir). Lección (memoria): medir SIEMPRE multi-seed a 20-40k — este sistema es bimodal y el single-seed/horizonte corto engaña (un single-seed a 8k sugirió falsamente que ayudaba).
- 2026-06-15: auditoría (2ª pasada) — (BUG) el SEGUIMIENTO del organismo seleccionado detectaba "sigue siendo el mismo individuo" por `lineage`+`generation`, pero DOS HERMANOS los comparten → si un hermano reutilizaba el slot del seleccionado, el inspector saltaba a él creyéndolo el mismo bicho. Ahora usa `serialOf` (id ÚNICO por organismo); retirados `selLineage`/`selGeneration` (muertos). (PERF, sim, behavior-preserving — checksum byte-idéntico a 1500t) el bucle de percepción precalcula el wrap+base de FILA por `oy` (no por celda); pastoreo y carroñeo reutilizan `tcell` (celda tras moverse, ya calculada) en vez de recomputar `cellIndexAt` (−2 por agente/tick). Benefician sobre todo máx. velocidad / barridos headless (el motor a 20 t/s ya va sobrado; el cuello real de FPS es el render).
- 2026-06-15: auditoría de código — FIXES de CORRECCIÓN. (1) Los sliders **(UI) que se EXPRESAN en el fenotipo** (costes, masa, velocidad, visión, eficiencias) solo afectaban a las CRÍAS futuras (el fenotipo se cachea al nacer y el worker no recomputaba en `'set'`) → contradecía "los (UI) afectan EN VIVO" (CLAUDE.md #4 / SPEC §6). Ahora el worker re-expresa a TODOS los vivos al cambiar un slider no-render (`sim.recomputePhenotypes()`; NO toca E/edad/posición/memoria/`bodyMatter` → conserva, medido 0% de salto). (2) Bug del POOL: un slot liberado por una muerte y REUTILIZADO por una cría EN EL MISMO TICK (pila LIFO) se re-procesaba en su tick de nacimiento (el guard `!alive` no basta porque el slot "revive") → filtrado por marca de agua de serial (medido: 88 casos/2000 ticks → 0). (3) WRAP toroidal del hash: con `scanR` 2-3 (visión larga) el wrap de UNA sola celda fallaba en la costura del toro (percepción y `_findMate`) → módulo correcto. (4) `repro.mateRadius` (slider hasta 150 u) superaba el tamaño de celda del hash (80) → `_findMate` (3×3 fijo) perdía parejas válidas → escaneo ADAPTATIVO (como la percepción). Smoke conserva (deriva −0.0001%), verificado en vivo.
- 2026-06-15: auditoría — PERF/limpieza. (a) `_refreshGrass`: la capa de food (≈50k px + blur toroidal) se recomputaba en CADA refresco, incluido el paneo/zoom SIN avance de tick (trabajo tirado: el food no cambió) → ahora se GATEA (`foodDirty`: avanzó el tick · cambió un slider de vegetación · realloc · capa estática recacheada) y el buffer world-space `_abyssSmooth` se REUTILIZA, re-teselando solo con la cámara → paneo/zoom barato (output pixel-idéntico; verificado en preview: sin costuras). (b) `charts._drawBiomass`: apilado O(bandas²·n)→O(bandas·n) (techos acumulados precalculados en scratch reutilizable). (c) `worker.speciesOf` se dimensiona al POOL REAL (`sim.cap`), no a `maxAgents` → evita escritura fuera de rango si `world.size`>1000 al arrancar.
- 2026-06-14: UI — la **pecera (ecosistema cerrado) es ya PERMANENTE**: se retiró el toggle "Ecosistema cerrado (pecera)" y la sección "🫧 Pecera sellada (materia)" (quedaba vacía). `config.world.closedMatter` sigue `true` por defecto y el modo abierto NO se borra del código (solo reactivable editando `config.js`). El slider **Fotosíntesis (pecera)** se mueve a "🍃 Comida y recurso" como PRIMER control (es el regulador principal de comida del mundo). Consecuencia del atenuado por modo: los controles de modo ABIERTO ("Comida disponible (rebrote)", "Reciclaje de cadáveres → pasto") quedan ahora SIEMPRE atenuados (inertes). Solo UI.
- 2026-06-14: fix render — las capas DECORATIVAS ancladas al mundo (PLANCTON + nieve marina) se quedaban ATRAPADAS en el cuadrante del tamaño VIEJO al subir `world.size` y Reiniciar (se sembraban una sola vez en el constructor sobre `world.size`=1000 y nada las re-sembraba). Ahora, cuando `world.size` cambia (main.js, mensaje `'world'`), se RE-SIEMBRAN sobre el nuevo tamaño — con el nº de motas escalado al área (densidad de plancton ~constante, Modelo A) — y la cámara se recentra. Solo render (no toca la sim). En reseed del mismo tamaño no se toca nada.
- 2026-06-14: UI — más reorganización del panel: **Zoom** pasa a justo debajo de Velocidad (controles de vista juntos, arriba); el botón **Simple/Laboratorio** se mueve al *readout*: los stats van en un bloque propio (`.readout-stats`) que envuelve internamente y el botón queda de HERMANO a la derecha, centrado vertical (bajo "contemplar") — así no cae una fila debajo (lab) ni infla el salto entre las dos líneas de texto (simple). `applyMode` reubica "Calidad" al `btn-row` en simple (antes lo anclaba junto al botón de modo, que ya no vive ahí); en el texto del readout, **nutriente** se sustituye por **mundo** (tamaño del mundo en unidades; el nutriente libre sigue en la curva de biomasa).
- 2026-06-14: UI — reorganización del panel: el slider de **Velocidad** sube arriba del todo (bajo el título), como control principal; **Materia total** se mueve de "Pecera sellada" a "Población y sembrado" (junto a Tamaño del mundo, conservando el hint de escala). ("Diversidad inicial" ya vivía en Población.)
- 2026-06-14: UI — los sliders que ESCALAN con el mundo (Materia total, Tope de población, Sembrado inicial) muestran una anotación tenue **"base → efectivo"** (p.ej. `60000 → 240k`) que se actualiza EN VIVO al mover Tamaño del mundo/pool, usando los valores PENDIENTES. Solo aparece si la escala ≠ 1 (a tamaño 1000, sin ruido); además comunica el TECHO (al toparse, el efectivo deja de crecer). Mata la confusión "pongo 60k pero el mundo tiene 240k" sin romper el thumb (que sigue en base). Solo UI.
- 2026-06-14: el ECOSISTEMA ESCALA con el tamaño del mundo (**Modelo A**) — `matterBudget` (×área), `pop.maxAgents` (×área, con techo `pop.maxAgentsCeiling` 8000 por perf), `gridCols/Rows` (×size → tamaño de celda constante) y los fundadores (×área) escalan JUNTOS con `world.size`. Mundo grande = ecosistema proporcionalmente mayor, MISMA densidad/dinámica, conservación EXACTA (medido: materia total = presupuesto escalado, ratio 1.000, deriva 0%; antes la siembra escalada CREABA materia → 89k vs 60k a size 2000 — bug corregido). Lo INTENSIVO (talla, sensores, velocidades, tasas, costes) NO escala. Por encima del techo de pool, `world.size` solo DISPERSA. + sembrado inicial escala con el área y se expone como slider; diversidad inicial movida al lab.
- 2026-06-14: rejilla de recurso CUADRADA — `gridCols`/`gridRows` 64×48 → **56×56** (celdas cuadradas en el mundo cuadrado, sin anisotropía). Elegido para conservar ≈ el nº de celdas y el ÁREA de celda (~313 u²) → no cambia la escala del campo de comida ni la capacidad de carga (cerrado = matterBudget).
- 2026-06-14: mundo CUADRADO de un solo valor — `world.width`/`world.height` (1200×800) → **`world.size`** (1000, toro cuadrado), expuesto como slider del lab "Tamaño del mundo" (`↻`, 400–5000). Es un DIAL DE DENSIDAD (medido headless): grande = disperso → menos depredación, MÁS especies (aislamiento espacial); pequeño = denso → más depredadores, menos especies. No cambia el alimento total (rejilla/materia fijos). Motor/render migrados a `world.size`.
- 2026-06-14: render — PISTA DEL LÍMITE DEL MUNDO (`render.worldBounds`, default ON, toggle en lab): hairline tenue (cian-frío) en los bordes de CADA tile del toro → se ve dónde acaba un mundo y empieza su repetición, sin barras vacías ni romper la inmersión. Es honesto con la topología (no inventa un borde) y ayuda a comprender el ecosistema. (Curiosidad: el límite se veía ANTES por accidente como la costura del sustrato — el "glitch borde del mundo" arreglado en 5bf8c57; ahora es deliberado y limpio.) Solo render.
- 2026-06-14: render — ZOOM mínimo = MUNDO ENTERO visible. La escala base pasa de "cover" (`Math.max` → llenaba la pantalla recortando un eje) a "fit/contain" (`Math.min` → a zoom 1 cabe el mundo completo; el eje sobrante lo rellena el toro en mosaico, sin barras vacías). `coverScale`→`fitScale` (canvas.js). Alinea el código con VISUAL §Responsive (que ya especificaba `min`; resuelve la contradicción cover↔contain). Solo render.
- 2026-06-14: docs — CONVENCIÓN DE UNIDADES explícita (raíz: auditoría de px↔tamaños). El motor trabaja en **unidades de mundo (u)**, no px de pantalla; la resolución de dibujo (DPR/`maxInternalPx`/backing) NO entra en la lógica ni en el tamaño aparente (se cancela: `aparente = radio·viewport/world·zoom`, solo nitidez); el tamaño absoluto en u es un **gauge libre** (escalar todas las longitudes por k y `moveCost` por 1/k² → idéntico) → solo importan los ratios. Cabecera dimensional en `config.js` + nota en SPEC §1 y VISUAL. CERO cambio de comportamiento (solo comentarios/docs).
- 2026-06-14: SUELO DE TALLA (`expr.size.min` 1.7→3.4) — fix del **TRÍO TRANSITORIO a largo plazo** (hallazgo de auditoría). Sin suelo, a >10k ticks la pop derivaba a cuerpos DIMINUTOS (size µ≈0.13) que, como r-estrategas, SATURABAN el pool (`maxAgents`, no la materia) y borraban la base de presa con talla → el gremio CAZADOR se extinguía (el trío solo se validaba a ~5-8k ticks). Con el suelo, la MATERIA vuelve a ligar la población (popSat ~50%, no 100%) y el cazador PERSISTE a 30k+ (medido headless 7/7 seeds; verificado en vivo). Conserva (deriva 0.014%). Coste asumido: criaturas algo más grandes (radio mínimo ×2) y más varianza entre siembras. (De paso: corregido el comentario de `expr.size` que decía falsamente "no afecta a la energía".)
- 2026-06-14: VISUAL.md — retirado el requisito de interpolar posiciones entre frames (el movimiento orgánico lo da la ondulación de nodos; el render dibuja el tick actual → decisión deliberada, fluido sin latigazo del toro).
- 2026-06-14: NUTRIENTE ESPACIAL en la pecera — el pool `N` pasa de escalar global a CAMPO por celda con difusión lenta (`world.nutrientDiffuse` 0.15) → manchas fértiles donde muere/respira algo (el pasto rebrota ahí), ciclo de nutrientes geográfico; conserva la materia exacta (medido). + VIZ (neblina índigo del nutriente). Medido headless: MEJORA la diversidad (gremios 2.5→3.0, H 0.81→1.02, cazadores ~4%→~18%: las manchas crean bolsas de presa densa donde cazar rinde) a costa de ~la mitad de población. El nacimiento reúne nutriente del vecindario 5×5.
- 2026-06-14: ROBUSTEZ del cazador (diagnóstico: muere 44% atacando, no de hambre → reducir FALLOS, no el coste de fallar) — (P1) el SEÑUELO ahora ATRAE a la presa que lo ve (`combat.lureAttract` 0.5) → emerge el cazador EMBOSCADA; (P2) nueva entrada sensorial de ESCAPABILIDAD de la presa (`BRAIN.I` 9→10, genoma 196→201). Medido headless: estabiliza al gremio cazador (fluctuación CV 0.47→0.28, supervivencia 5/6→6/6 a 8000t) sin cablear. (Antes se descartó `failDamage`↓ con datos: rompía el freno denso-dependiente → boom-bust.)
- 2026-06-14: tuning DIVERSIDAD de defaults (medido headless multi-seed) — `matterBudget` 30k→60k, `closedRegen`→0.004, `forageReach`→5, `massExp` 1.5→1.3, `k_haul`→0.2, `fleeSpeed`→1.0: de 2 gremios (herbívoro+carroñero, sin cazadores, talla monomodal) a **3 gremios con CAZADORES en 6/6 siembras + grupo de talla GRANDE emergente** (H 0.73→0.90, big% 0→9). Además: rangos de sliders del lab saneados (ningún default en un extremo).
- 2026-06-14: cerebro — 2 entradas sensoriales nuevas: COBERTURA local (vegetación de su celda) + TALLA relativa de la presa (`BRAIN.I` 7→9, genoma 186→196). Sembradas a peso ~0 → el uso táctico del refugio y evitar presa grande EMERGEN (no cableado). Verificado headless (conducta base intacta a 6000 ticks).
- 2026-06-14: locomoción — coste de ARRASTRE de la forma (`energy.k_drag` 0.4 · `dragRef` 1.1): el arrastre (`Dmul`) ahora ENCARECE el nado, no solo lo frena (cierra el incentivo del "arrastre gratis"); complementa `k_haul` (masa). Efecto sutil en el régimen lento por defecto, medido headless.
- 2026-06-14: móvil — fix: la cámara saltaba al soltar UN dedo de la pinza (re-anclaje de `lastX/lastY` al dedo que queda, en `pointerup`/`pointercancel`).
- 2026-06-14: 'Colorear por' → Rol — la banda de leyenda se PONDERA por los totales de cada oficio (viva, por frame) y suma un 4º oficio OMNÍVORO; criterio trófico unificado en `trophicRole` (la curva de población y el color comparten fuente).
- 2026-06-14: auditoría de código — perf: `Math.hypot`→`sqrt(a²+b²)` en el bucle caliente (≈11× más rápido) y snapshot del worker por TRANSFERIBLES (sin copia); docs: SPEC sincronizada con la pecera como modo por defecto; smoke test headless (`npm test`: invariantes + conservación de materia).
- 2026-06-14: ideas — `indice-ideas.md` procesado: bandeja de entrada vaciada (ideas crudas → pitch o resueltas); gráfica de biomasa y leyenda Rol marcadas hechas.
- 2026-06-13: UI — en el laboratorio, los controles de render (Calidad, Resolución, FPS máx, Caché de sprites) se agrupan en una sección "Rendimiento". El modo simple no cambia (Calidad sigue en su sitio).
- 2026-06-13: render — CACHÉ DE SPRITES por esqueleto (opt-in, default OFF, modo rendimiento): cachea cada organismo (cuerpo por nodo + ojos/señuelo) en un atlas y lo ENSAMBLA con la onda viva → conserva la ondulación → vale en todas las calidades (máxima incluida). Evicción por muerte (mantiene cacheados los vivos). ~1.7× por-agente. Toggle en el lab; knobs `spriteCache*` en config.
- 2026-06-13: render — halo del bulbo del señuelo ceñido (br×7→br×4): mismo aspecto, ~⅓ del coste.
- 2026-06-13: render — el LOD se desliga por completo de la resolución (`rPx = radio·zoom`, solo depende de calidad+zoom) y calidad MÁXIMA = sin LOD (todo a grafo completo).
- 2026-06-13: render — LOD: umbrales por nodo a config (`lodFlat`/`lodOutline`/`lodTexture`), ramp reordenado (el contorno va siempre con el grafo), `lodUltraMult` eliminado.
- 2026-06-12: RENDIMIENTO render (4K/móvil): cap de resolución interna `render.maxInternalPx` (escalar, def 1280, slider en el lab, todas las calidades) → render por debajo + reescalado CSS (4K ~8.6× más rápido); + bloom downsampled, halos por sprite, sustrato cacheado.
- 2026-06-12: RENDIMIENTO render: dibujado BAJO DEMANDA (solo redibuja si cambió tick/cámara/selección) + cap `render.maxFPS` (def 60).
- 2026-06-12: pecera ACTIVADA por defecto (`world.closedMatter`); se mantiene `pop.maxAgents 2000` (la pop se autorregula por la materia, sin saturar).
- 2026-06-12: pecera — red trófica completa por defecto (herbívoros+carroñeros+cazadores): `closedRegen 0.0034`, `maxAgents 2000`, `fleeSpeed 1.2`, `scavPenalty 0.30` (trío estable 4/6 siembras).
- 2026-06-12: pecera — `pop.carnivoreSeedFrac 0.20`; tope de población y siembra ajustables en el lab.
- 2026-06-12: ECOSISTEMA CERRADO EN MATERIA (`world.closedMatter`): pecera de materia constante (nacer consume nutriente → capacidad de carga endógena; conserva, validado headless). `closedRegen` y `matterBudget` en el lab; N libre en la cabecera.
- 2026-06-12: móvil — reabrir el menú arrastrando hacia arriba la barra de velocidad; ocultar la hoja deslizándola hacia abajo (touch).
- 2026-06-12: 'Colorear por' → Rol (herbívoro/carroñero/cazador = argmax effHerb/effHunt/effScav); leyenda de color visible en el lab.
- 2026-06-12: gráfica de población — serie de CARROÑEROS (effScav>effHunt), separada de cazadores; color por GEN unificado a verde→amarillo→rojo (histograma/organismos/leyenda).
- 2026-06-12: histograma — `scav` y `temp_pref` añadidos al selector (12 genes funcionales).
- 2026-06-12: auditoría UI — expuestos `k_grazeWide`, `morphReach`, `corpseReturn`; atajos de teclado ocultos en táctil; readout común simple/lab (`fps · t/s · pob`).
- 2026-06-12: modo simple más simple (sin gráfica ni semilla; móvil entra a zoom 1.8×); fix — el retrato del inspector dibuja a detalle completo (`_forceFull`).
- 2026-06-12: `vercel.json` — `Cache-Control: must-revalidate` (evita mezclar módulos viejos+nuevos tras un deploy).
- 2026-06-12: GUSANO carroñero (Fase 2) — gen `scav` (cazar↔carroñear, genoma →186), `scavPenalty`/`k_scavThin`, siembra proto-gusano.
- 2026-06-11: CARROÑA (Fase 1) — campo `carrion` por celda (cuerpos al morir, decaen al pasto, los come quien come carne).
- 2026-06-11: `energy.carcassValue` (def 0.25) — la caza rinde la BIOMASA de la presa además de su energía (arregla cazar en escasez).
- 2026-06-11: render — la animación de nodos sigue la velocidad ABSOLUTA (lentos ondean suave, rápidos baten fuerte).
- 2026-06-11: lab — demografía de todo el ecosistema en 2 gráficas (nacimientos sexual/asexual · muertes por causa); retirada la monitorización de carnívoros.
- 2026-06-11: retirado el match color↔luz (`color.matchPenalty`) — era selección invisible; `hue` pasa a gen neutro/linaje.
- 2026-06-11: la velocidad importa — `combat.fleeSpeed 2` + `refuge.strength 0.3` (escape por velocidad relativa).
- 2026-06-11: `repro.asexual` se mantiene ON (solo-sexual aplana la diversidad de talla).
- 2026-06-11: forrajeo por talla (`resource.forageReach` 3) + `omniPenalty` 0.15 → dos grupos de talla robustos.
- 2026-06-11: coste de transporte ∝ masa (`energy.k_haul`).
- 2026-06-11: `combat.preyBandLo` expuesto en la UI (banda de caza por talla).
- 2026-06-11: `headThrust` 0.15→0.06 (nadar exige propulsores, no la cabeza).
- 2026-06-11: docs reestructuradas — `CONFIG.md` retirado (config.js fuente única); ideas → `ideas/`; AUDIT a `archivo/`.
- 2026-06-11: morfología Capas 1-3 — `tipShape` (forma), `morphReach`/`k_grazeWide` (función), `gaitMode`+`flapCost` (movimiento); genoma 169→185.
- 2026-06-11: 3ª calidad "Máxima" (doble bloom, supersampling, +nieve); botón Reiniciar a la cabecera (ámbar).
- 2026-06-11: `diet.omniPenalty` 0→0.05 (dial de especialización).
- 2026-06-10: eliminado el escenario pradera → Cenote abisal único; quitadas las estelas.
- 2026-06-10: modo simple simplificado; gráfica de población por dieta.
- 2026-06-10: SPEC/VISUAL reescritos al modelo de nodos (deuda v2.0 saldada).
- 2026-06-09: auditoría evolutiva CERRADA (14/14) → `archivo/AUDIT_EVOLUCION.md`.
